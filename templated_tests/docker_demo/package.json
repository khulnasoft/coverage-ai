{
  "name": "docker_demo",
  "version": "1.0.0",
  "description": "Docker containerized web application demo",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'Building static assets...' && mkdir -p dist && cp index.html dist/ && cp style.css dist/",
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  },
  "keywords": ["docker", "demo", "web", "container"],
  "author": "Coverage AI",
  "license": "MIT"
}
